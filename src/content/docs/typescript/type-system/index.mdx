---
title: 型別系統
description: TypeScript 的型別系統與其他語言有什麼根本差異。
order: 2
sidebarLabel: 型別系統
---

這一節處理的是「TypeScript 的型別系統長什麼樣子」，
而不是「有哪些語法可以用」。

如果你從 Java、C# 或 Rust 過來，這裡的內容特別重要——
TypeScript 的型別檢查方式與那些語言在根本上不同，
沿用舊的直覺會在某些地方失效得很安靜。
