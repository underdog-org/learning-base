---
title: GSAP
description: 動畫的核心不是屬性，是時間。
order: 0
sidebarLabel: 總覽
---

GSAP 的 API 表面很小——`gsap.to()`、`gsap.timeline()`，幾乎就是全部。
難的不是記住這些函式，而是建立對「時間軸」的直覺。

## 這裡的取徑

本主題的每一篇都會附上可拖動的參數面板。
理由是：閱讀 `ease: "power2.out"` 這串文字無法讓你理解它的形狀，
但拖動一次滑桿、看曲線如何改變，三秒鐘就懂了。

這也是本站互動元件的設計原則——
**能用控制變數解釋的事，不要用文字解釋。**
